'------------------------------------------------------------------------------
' <auto-generated>
'     Este código se generó a partir de una plantilla.
'
'     Los cambios en este archivo pueden ocasionar un comportamiento incorrecto y se perderán si
'     el código se vuelve a generar.
' </auto-generated>
'------------------------------------------------------------------------------

Imports System
Imports System.Collections.Generic
Imports System.Collections.ObjectModel
Imports System.Collections.Specialized
Imports System.ComponentModel
Imports System.Globalization
Imports System.Runtime.Serialization
Imports System.Runtime.CompilerServices

<DataContract(IsReference:=True)>
<KnownType(GetType(ActivosFijos))>
<KnownType(GetType(Leasing))>
Partial Public Class DetalleLeasingActivosFijos
    Implements IObjectWithChangeTracker
    Implements INotifyPropertyChanged

#Region "Propiedades primitivas"


	<DataContract()> Partial Public Structure PropertyNames
				public shared cct_Id As string = "cct_Id"
				public shared tdo_Id As string = "tdo_Id"
				public shared dtd_Id As string = "dtd_Id"
				public shared lea_Serie As string = "lea_Serie"
				public shared lea_Numero As string = "lea_Numero"
				public shared lea_Item As string = "lea_Item"
				public shared ACF_ID As string = "ACF_ID"
				public shared ACF_COR_INCIDENCIA As string = "ACF_COR_INCIDENCIA"
				public shared dele_valor As string = "dele_valor"
		    End Structure
	



    <DataMember()>
    Public Property cct_Id() As String
        Get
            Return _cct_Id
        End Get
        Set(ByVal value As String)
            If Not Equals(_cct_Id, value) Then
                If ChangeTracker.ChangeTrackingEnabled AndAlso ChangeTracker.State <> ObjectState.Added Then
                    Throw New InvalidOperationException("La propiedad 'cct_Id' forma parte de la clave del objeto y no se puede modificar. Solo se pueden realizar cambios en las propiedades de clave cuando no se realiza un seguimiento del objeto o su estado es Agregado.")
                End If
                If Not IsDeserializing Then
                    If Leasing IsNot Nothing AndAlso Not Equals(Leasing.cct_Id, value) Then
                        Leasing = Nothing
                    End If
                End If
                _cct_Id = value
                OnPropertyChanged("cct_Id")
            End If
        End Set
    End Property

    Private _cct_Id As String

    <DataMember()>
    Public Property tdo_Id() As String
        Get
            Return _tdo_Id
        End Get
        Set(ByVal value As String)
            If Not Equals(_tdo_Id, value) Then
                If ChangeTracker.ChangeTrackingEnabled AndAlso ChangeTracker.State <> ObjectState.Added Then
                    Throw New InvalidOperationException("La propiedad 'tdo_Id' forma parte de la clave del objeto y no se puede modificar. Solo se pueden realizar cambios en las propiedades de clave cuando no se realiza un seguimiento del objeto o su estado es Agregado.")
                End If
                If Not IsDeserializing Then
                    If Leasing IsNot Nothing AndAlso Not Equals(Leasing.tdo_Id, value) Then
                        Leasing = Nothing
                    End If
                End If
                _tdo_Id = value
                OnPropertyChanged("tdo_Id")
            End If
        End Set
    End Property

    Private _tdo_Id As String

    <DataMember()>
    Public Property dtd_Id() As String
        Get
            Return _dtd_Id
        End Get
        Set(ByVal value As String)
            If Not Equals(_dtd_Id, value) Then
                If ChangeTracker.ChangeTrackingEnabled AndAlso ChangeTracker.State <> ObjectState.Added Then
                    Throw New InvalidOperationException("La propiedad 'dtd_Id' forma parte de la clave del objeto y no se puede modificar. Solo se pueden realizar cambios en las propiedades de clave cuando no se realiza un seguimiento del objeto o su estado es Agregado.")
                End If
                If Not IsDeserializing Then
                    If Leasing IsNot Nothing AndAlso Not Equals(Leasing.dtd_Id, value) Then
                        Leasing = Nothing
                    End If
                End If
                _dtd_Id = value
                OnPropertyChanged("dtd_Id")
            End If
        End Set
    End Property

    Private _dtd_Id As String

    <DataMember()>
    Public Property lea_Serie() As String
        Get
            Return _lea_Serie
        End Get
        Set(ByVal value As String)
            If Not Equals(_lea_Serie, value) Then
                If ChangeTracker.ChangeTrackingEnabled AndAlso ChangeTracker.State <> ObjectState.Added Then
                    Throw New InvalidOperationException("La propiedad 'lea_Serie' forma parte de la clave del objeto y no se puede modificar. Solo se pueden realizar cambios en las propiedades de clave cuando no se realiza un seguimiento del objeto o su estado es Agregado.")
                End If
                If Not IsDeserializing Then
                    If Leasing IsNot Nothing AndAlso Not Equals(Leasing.lea_Serie, value) Then
                        Leasing = Nothing
                    End If
                End If
                _lea_Serie = value
                OnPropertyChanged("lea_Serie")
            End If
        End Set
    End Property

    Private _lea_Serie As String

    <DataMember()>
    Public Property lea_Numero() As String
        Get
            Return _lea_Numero
        End Get
        Set(ByVal value As String)
            If Not Equals(_lea_Numero, value) Then
                If ChangeTracker.ChangeTrackingEnabled AndAlso ChangeTracker.State <> ObjectState.Added Then
                    Throw New InvalidOperationException("La propiedad 'lea_Numero' forma parte de la clave del objeto y no se puede modificar. Solo se pueden realizar cambios en las propiedades de clave cuando no se realiza un seguimiento del objeto o su estado es Agregado.")
                End If
                If Not IsDeserializing Then
                    If Leasing IsNot Nothing AndAlso Not Equals(Leasing.lea_Numero, value) Then
                        Leasing = Nothing
                    End If
                End If
                _lea_Numero = value
                OnPropertyChanged("lea_Numero")
            End If
        End Set
    End Property

    Private _lea_Numero As String

    <DataMember()>
    Public Property lea_Item() As String
        Get
            Return _lea_Item
        End Get
        Set(ByVal value As String)
            If Not Equals(_lea_Item, value) Then
                If ChangeTracker.ChangeTrackingEnabled AndAlso ChangeTracker.State <> ObjectState.Added Then
                    Throw New InvalidOperationException("La propiedad 'lea_Item' forma parte de la clave del objeto y no se puede modificar. Solo se pueden realizar cambios en las propiedades de clave cuando no se realiza un seguimiento del objeto o su estado es Agregado.")
                End If
                _lea_Item = value
                OnPropertyChanged("lea_Item")
            End If
        End Set
    End Property

    Private _lea_Item As String

    <DataMember()>
    Public Property ACF_ID() As String
        Get
            Return _aCF_ID
        End Get
        Set(ByVal value As String)
            If Not Equals(_aCF_ID, value) Then
                ChangeTracker.RecordOriginalValue("ACF_ID", _aCF_ID)
                If Not IsDeserializing Then
                    If ActivosFijos IsNot Nothing AndAlso Not Equals(ActivosFijos.ACF_ID, value) Then
                        ActivosFijos = Nothing
                    End If
                End If
                _aCF_ID = value
                OnPropertyChanged("ACF_ID")
            End If
        End Set
    End Property

    Private _aCF_ID As String

    <DataMember()>
    Public Property ACF_COR_INCIDENCIA() As Decimal
        Get
            Return _aCF_COR_INCIDENCIA
        End Get
        Set(ByVal value As Decimal)
            If Not Equals(_aCF_COR_INCIDENCIA, value) Then
                ChangeTracker.RecordOriginalValue("ACF_COR_INCIDENCIA", _aCF_COR_INCIDENCIA)
                If Not IsDeserializing Then
                    If ActivosFijos IsNot Nothing AndAlso Not Equals(ActivosFijos.ACF_COR_INCIDENCIA, value) Then
                        ActivosFijos = Nothing
                    End If
                End If
                _aCF_COR_INCIDENCIA = value
                OnPropertyChanged("ACF_COR_INCIDENCIA")
            End If
        End Set
    End Property

    Private _aCF_COR_INCIDENCIA As Decimal

    <DataMember()>
    Public Property dele_valor() As Decimal
        Get
            Return _dele_valor
        End Get
        Set(ByVal value As Decimal)
            If Not Equals(_dele_valor, value) Then
                _dele_valor = value
                OnPropertyChanged("dele_valor")
            End If
        End Set
    End Property

    Private _dele_valor As Decimal

#End Region
#Region "Propiedades de navegación"

    <DataMember()>
    Public Property ActivosFijos() As ActivosFijos
        Get
            Return _activosFijos
        End Get
        Set(ByVal value As ActivosFijos)
            If _activosFijos IsNot value Then
                Dim previousValue As ActivosFijos = _activosFijos
                _activosFijos = value
                FixupActivosFijos(previousValue)
                OnNavigationPropertyChanged("ActivosFijos")
            End If
        End Set
    End Property

    Private _activosFijos As ActivosFijos


    <DataMember()>
    Public Property Leasing() As Leasing
        Get
            Return _leasing
        End Get
        Set(ByVal value As Leasing)
            If _leasing IsNot value Then
                If ChangeTracker.ChangeTrackingEnabled AndAlso ChangeTracker.State <> ObjectState.Added AndAlso value IsNot Nothing Then
                    ' Este es el extremo dependiente de una relación de identificación por lo que el extremo principal no se puede cambiar si ya está establecido;
                    ' de lo contrario, solo se puede establecer en una entidad con una clave primaria que tenga el mismo valor que la clave externa del extremo dependiente.
                    If Not Equals(cct_Id, value.cct_Id) OrElse Not Equals(tdo_Id, value.tdo_Id) OrElse Not Equals(dtd_Id, value.dtd_Id) OrElse Not Equals(lea_Serie, value.lea_Serie) OrElse Not Equals(lea_Numero, value.lea_Numero) Then
                        Throw New InvalidOperationException("El extremo principal de una relación de identificación solo se puede modificar cuando el extremo dependiente se encuentra en el estado agregado.")
                    End If
                End If
                Dim previousValue As Leasing = _leasing
                _leasing = value
                FixupLeasing(previousValue)
                OnNavigationPropertyChanged("Leasing")
            End If
        End Set
    End Property

    Private _leasing As Leasing


#End Region
#Region "ChangeTracking"

    Protected Overridable Sub OnPropertyChanged(ByVal propertyName As String)
        If ChangeTracker.State <> ObjectState.Added AndAlso ChangeTracker.State <> ObjectState.Deleted Then
            ChangeTracker.State = ObjectState.Modified
        End If
        RaiseEvent PropertyChanged(Me, New PropertyChangedEventArgs(propertyName))
    End Sub

    Protected Overridable Sub OnNavigationPropertyChanged(ByVal propertyName As String)
        RaiseEvent PropertyChanged(Me, New PropertyChangedEventArgs(propertyName))
    End Sub

    Private Event PropertyChanged As PropertyChangedEventHandler Implements INotifyPropertyChanged.PropertyChanged
    Private _changeTracker As ObjectChangeTracker

    <DataMember()>
    Public Property ChangeTracker() As ObjectChangeTracker Implements IObjectWithChangeTracker.ChangeTracker
        Get
            If _changeTracker Is Nothing Then
                _changeTracker = New ObjectChangeTracker()
                AddHandler _changeTracker.ObjectStateChanging, AddressOf HandleObjectStateChanging
            End If
            Return _changeTracker
        End Get
        Set(ByVal value As ObjectChangeTracker)
            If _changeTracker IsNot Nothing Then
                RemoveHandler _changeTracker.ObjectStateChanging, AddressOf HandleObjectStateChanging
            End If
            _changeTracker = value
            If _changeTracker IsNot Nothing Then
                AddHandler _changeTracker.ObjectStateChanging, AddressOf HandleObjectStateChanging
            End If
        End Set
    End Property

    Private Sub HandleObjectStateChanging(ByVal sender As Object, ByVal e As ObjectStateChangingEventArgs)
        If e.NewState = ObjectState.Deleted Then
            Me.ClearNavigationProperties()
        End If
    End Sub

    ' Este tipo de entidad es el extremo dependiente en al menos una asociación que realiza eliminaciones en cascada.
    ' Este controlador de eventos procesará notificaciones que tienen lugar cuando se elimina el extremo principal.
    Friend Sub HandleCascadeDelete(ByVal sender As Object, ByVal e As ObjectStateChangingEventArgs)
        If e.NewState = ObjectState.Deleted Then
            Me.MarkAsDeleted()
        End If
    End Sub

    Private _isDeserializing As Boolean
    Protected Property IsDeserializing() As Boolean
        Get
            Return _isDeserializing
        End Get
        Private Set(ByVal value As Boolean)
            _isDeserializing = value
        End Set
    End Property

    <OnDeserializing()>
    Public Sub OnDeserializingMethod(ByVal context As StreamingContext)
        IsDeserializing = True
    End Sub

    <OnDeserialized()>
    Public Sub OnDeserializedMethod(ByVal context As StreamingContext)
        IsDeserializing = False
        ChangeTracker.ChangeTrackingEnabled = True
    End Sub

    Protected Overridable Sub ClearNavigationProperties()
        ActivosFijos = Nothing
        Leasing = Nothing
    End Sub

#End Region
#Region "Corrección de asociación"

    Private Sub FixupActivosFijos(ByVal previousValue As ActivosFijos)
        If IsDeserializing Then
            Return
        End If

        If previousValue IsNot Nothing AndAlso previousValue.DetalleLeasingActivosFijos.Contains(Me) Then
            previousValue.DetalleLeasingActivosFijos.Remove(Me)
        End If

        If ActivosFijos IsNot Nothing Then
            If Not ActivosFijos.DetalleLeasingActivosFijos.Contains(Me) Then
                ActivosFijos.DetalleLeasingActivosFijos.Add(Me)
            End If

            ACF_ID = ActivosFijos.ACF_ID
            ACF_COR_INCIDENCIA = ActivosFijos.ACF_COR_INCIDENCIA
        End If
        If ChangeTracker.ChangeTrackingEnabled Then
            If ChangeTracker.OriginalValues.ContainsKey("ActivosFijos") AndAlso
                ChangeTracker.OriginalValues("ActivosFijos") Is ActivosFijos Then
                ChangeTracker.OriginalValues.Remove("ActivosFijos")
            Else
                ChangeTracker.RecordOriginalValue("ActivosFijos", previousValue)
            End If
            If ActivosFijos IsNot Nothing AndAlso Not ActivosFijos.ChangeTracker.ChangeTrackingEnabled Then
                ActivosFijos.StartTracking()
            End If
        End If
    End Sub

    Private Sub FixupLeasing(ByVal previousValue As Leasing)
        If IsDeserializing Then
            Return
        End If

        If previousValue IsNot Nothing AndAlso previousValue.DetalleLeasingActivosFijos.Contains(Me) Then
            previousValue.DetalleLeasingActivosFijos.Remove(Me)
        End If

        If Leasing IsNot Nothing Then
            If Not Leasing.DetalleLeasingActivosFijos.Contains(Me) Then
                Leasing.DetalleLeasingActivosFijos.Add(Me)
            End If

            cct_Id = Leasing.cct_Id
            tdo_Id = Leasing.tdo_Id
            dtd_Id = Leasing.dtd_Id
            lea_Serie = Leasing.lea_Serie
            lea_Numero = Leasing.lea_Numero
        End If
        If ChangeTracker.ChangeTrackingEnabled Then
            If ChangeTracker.OriginalValues.ContainsKey("Leasing") AndAlso
                ChangeTracker.OriginalValues("Leasing") Is Leasing Then
                ChangeTracker.OriginalValues.Remove("Leasing")
            Else
                ChangeTracker.RecordOriginalValue("Leasing", previousValue)
            End If
            If Leasing IsNot Nothing AndAlso Not Leasing.ChangeTracker.ChangeTrackingEnabled Then
                Leasing.StartTracking()
            End If
        End If
    End Sub

#End Region
End Class
